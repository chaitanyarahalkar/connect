[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/dashboard/Dockerfile"
watchPatterns = ["apps/dashboard/**", "packages/shared/**"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
